# Mock mode is deterministic, needs no credentials, and is safe for a public demo.
MOCK=1
HOST=127.0.0.1
PORT=8000

# Live model mode. Prefer an AWS runtime with an attached IAM role.
# MOCK=0
# LLM_PROVIDER=bedrock
# AWS_REGION=us-east-1
# BEDROCK_MODEL=us.anthropic.claude-haiku-4-5-20251001-v1:0
