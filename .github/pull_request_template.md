## What changed

## Why

## Checks

- [ ] `make check` passes
- [ ] `make demo` re-run and `web/demo/frames.json` committed, if the scenario, fixtures or graph changed
- [ ] New settings are in `.env.example` and `docs/configuration.md`
